query getUserConversions($user: String, $skip: Int!, $pageSize: Int!) {
  conversions(where: { user: $user }, first: $pageSize, skip: $skip) {
    bAsset {
      symbol
    }
    bassetQuantity
    massetQuantity
    type
    transaction {
      id
      timestamp
    }
  }
}
