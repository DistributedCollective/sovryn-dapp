export enum TabSize {
  small = 'small',
  normal = 'normal',
}

export enum TabStyle {
  primary = 'primary',
  secondary = 'secondary',
}

export enum TabColor {
  default = 'default',
  buy = 'buy',
  sell = 'sell',
}
